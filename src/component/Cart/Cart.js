import React from 'react';

const Cart = (props) => {
    return (
        <div className='cart'>
             
               <p>name:{props.item.name}</p>
             

               
        </div>
    );
};

export default Cart;